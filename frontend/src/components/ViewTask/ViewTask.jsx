import React from "react";
import classes from "./ViewTask.module.scss";
import Modal from "../Modal/Modal";
const ViewTask = () => {
  return (
    <Modal>
      <section className={classes.view_task}>ViewTask</section>
    </Modal>
  );
};

export default ViewTask;
